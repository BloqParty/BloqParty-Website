const c = require(`./colors`)

module.exports = [
    [`debug`, c.bold, c.cyan], 
    [`info`, c.bold, c.white], 
    [`error`, c.bold, c.red], 
    [`warn`, c.bold, c.yellow], 
    [`log`, c.bold, c.white],
    [`main`, c.bold, c.magenta],
];